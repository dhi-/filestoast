{
  "name": "filestoast",
  "version": "0.1.0",
  "description": "A utility library that takes a glob of javascript files and creates an array containing the file sources, their paths and the ast representations of those js files using the esprima parser.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dhi",
  "license": "MIT",
  "dependencies": {
    "acorn": "^0.8.0",
    "esprima": "^1.2.2",
    "map-stream": "^0.1.0",
    "vinyl-fs": "^0.3.7"
  }
}
